import React from "react";
import ReactDOM from "react-dom/client";
import { Super5App } from "./Super5App";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <Super5App />
  </React.StrictMode>
);
